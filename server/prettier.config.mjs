const config = {
  semi: true,
  tabWidth: 2,
  useTabs: false,
  printWidth: 120,
  singleQuote: true,
  trailingComma: 'all',
  proseWrap: 'always',
  editor: {
    insertSpaces: true,
  },
};

export default config;
